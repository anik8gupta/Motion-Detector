# Motion-Detector
Full fetched Motion Detection web application using Flask

## How to Run 
1. Install **Flask**:

    ```
    pip install flask
    ```

2. Run the app:

    ```
    python server.py
    ```
